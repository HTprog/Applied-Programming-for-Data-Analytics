#The variable called "first" contains the input function that asks for the user's first name
first=input("What is your first name?: ")
#The variable "last" contains an input function that asks for the user's surname
last=input("What is your surname?: ")
#The line below uses the print function to combine the string "Hello", the variable "first" and the variable "last"
print("Hello "+first+" "+last)